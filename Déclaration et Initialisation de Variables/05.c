#include <stdio.h>

int main() {
    int note1 = 85;
    int note2 = 90;
    int note3 = 78;
    float moyenne = (note1 + note2 + note3) / 3.0;
    printf("La moyenne des notes est : %.2f\n", moyenne);

    return 0;
}
